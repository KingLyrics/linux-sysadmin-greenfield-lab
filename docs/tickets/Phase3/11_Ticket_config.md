# Steps to configure the server ssh

1) Ensure that SSH is installed 
2) Ensure that it is enabled
3) Modify SSH config files 
4) Login to the SSH server

- Since on a virtual machine configure to run only with NAT the host port was 22 and guest port 22 to simulate an actual ssh port.

## Outcome
SSH access was established securely with controlled authentication and full logging visibility.