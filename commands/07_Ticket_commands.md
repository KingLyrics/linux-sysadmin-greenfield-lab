# Commands ran
-`chmod g+s /path/to/directory` - To ensure that new files in directory has inheritence
- `chown :groupname directory` - Group owns a directory
- `chmod 760` - For each group Owner can rwx, group memebers can rw, other can do nothing.
- `sudo gpasswd -d username groupname` - Remove from group
- `chmod 770` - was the solution to the problem

